name: my_pumpkin
version: 1.0.0

profile: my_pumpkin

clean-targets:
  - target
  - dbt_packages
  - dev.duckdb

seeds:
  my_pumpkin:
    sources:
      +schema: sources

snapshots:
  my_pumpkin:
    sources:
      +target_schema: sources

models:
  my_pumpkin:
